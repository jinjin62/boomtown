const styles = theme => ({
  root: {
    display: 'flex',
    flexWrap: 'wrap',
    flexGrow: 1
  },
  card: {
    height: 140,
    width: '100%'
  },
  control: {
    padding: theme.spacing.unit * 2
  }
});

export default styles;
